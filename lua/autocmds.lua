local M = {}

-- vim.api.nvim_create_autocmd("CursorMoved", {
--     callback = function()
--         -- Write to message history (visible with :messages)
--         vim.api.nvim_echo({{"CursorMoved", "Comment"}}, true, {})
--     end,
-- })
--
-- vim.api.nvim_create_autocmd("CursorHold", {
--     callback = function()
--         -- Write to message history (visible with :messages)
--         vim.api.nvim_echo({{"CursorHold", "Comment"}}, true, {})
--     end,
-- })

return M
